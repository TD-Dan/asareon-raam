{
  "feature_name":"session",
  "summary":"Acts as a pure, passive data manager for multiple, concurrent session transcripts (the 'Public Ledger').",
  "listensFor":[
    {
      "action_name":"session.CREATE",
      "summary":"Creates a new, empty session transcript. If the name exists, a numeric suffix will be added.",
      "payload_schema":{
        "type":"object",
        "properties":{
          "name":{
            "type":"string",
            "description":"An optional user-friendly name for the new session."
          },
          "isHidden":{
            "type":"boolean",
            "description":"If true, the session is created in a hidden state (e.g., for agent p-cognition). Defaults to false."
          },
          "isAgentPrivate":{
            "type":"boolean",
            "description":"If true, the session is marked as an agent's private cognition session. Defaults to false."
          }
        }
      }
    },
    {
      "action_name": "session.CLONE",
      "summary": "Creates a new session by cloning an existing one, including its entire ledger. The clone is always non-hidden regardless of source.",
      "payload_schema": {
        "type": "object",
        "properties": {
          "session": {
            "type": "string",
            "description": "The ID of the session to clone."
          }
        },
        "required": [ "session" ]
      }
    },
    {
      "action_name":"session.UPDATE_CONFIG",
      "summary":"Updates the configuration of an existing session. Targets the session via the 'session' property (ID or name).",
      "payload_schema":{
        "type":"object",
        "properties":{
          "session":{
            "type":"string",
            "description":"The ID or name of the session to update."
          },
          "name":{
            "type":"string",
            "description":"The new name for the session. Will be de-duplicated if necessary."
          }
        },
        "required":[
          "session"
        ]
      }
    },
    {
      "action_name":"session.DELETE",
      "summary":"Deletes an entire session transcript. Targets the session via the 'session' property (ID or name).",
      "payload_schema":{
        "type":"object",
        "properties":{
          "session":{
            "type":"string",
            "description":"The ID or name of the session to delete."
          }
        },
        "required":[
          "session"
        ]
      }
    },
    {
      "action_name":"session.POST",
      "summary":"Posts a new entry to a specific session's ledger. Can be a text message or contain metadata for a UI component.",
      "payload_schema":{
        "type":"object",
        "properties":{
          "session":{
            "type":"string",
            "description":"The ID or name of the target session."
          },
          "senderId":{
            "type":"string"
          },
          "message":{
            "type":"string"
          },
          "messageId":{
            "type":"string",
            "description":"An optional, pre-generated ID for the message. If not provided, one will be created."
          },
          "metadata":{
            "type":"object",
            "description":"An optional, generic metadata object."
          },
          "afterMessageId": {
            "type": ["string", "null"],
            "description": "If provided, the new entry will be inserted directly after this message ID. If the ID is not found, it will be appended to the end."
          }
        },
        "required":[
          "session",
          "senderId"
        ]
      }
    },
    {
      "action_name":"session.UPDATE_MESSAGE",
      "summary":"Updates the raw content and metadata of an existing message. Blocked if the target message is locked.",
      "payload_schema":{
        "type":"object",
        "properties":{
          "session":{
            "type":"string",
            "description":"The ID or name of the target session."
          },
          "messageId":{
            "type":"string"
          },
          "newContent":{
            "type":"string"
          },
          "newMetadata":{
            "type":"object"
          }
        },
        "required":[
          "session",
          "messageId"
        ]
      }
    },
    {
      "action_name":"session.DELETE_MESSAGE",
      "summary":"Deletes a single message from a session's ledger. Blocked if the target message is locked.",
      "payload_schema":{
        "type":"object",
        "properties":{
          "session":{
            "type":"string",
            "description":"The ID or name of the session to delete."
          },
          "messageId":{
            "type":"string"
          }
        },
        "required":[
          "session",
          "messageId"
        ]
      }
    },
    {
      "action_name":"session.REQUEST_LEDGER_CONTENT",
      "summary":"Requests the formatted ledger content for a specific session. Used by agents to build context for a cognitive turn.",
      "payload_schema":{
        "type":"object",
        "properties":{
          "sessionId":{
            "type":"string"
          },
          "correlationId":{
            "type":"string"
          }
        },
        "required":[
          "sessionId",
          "correlationId"
        ]
      }
    },
    {
      "action_name":"session.SET_ACTIVE_TAB",
      "summary":"Sets the currently visible session in the main tabbed view. Targets the session via the 'session' property (ID or name).",
      "payload_schema":{
        "type":"object",
        "properties":{
          "session":{
            "type":"string",
            "description":"The ID or name of the session to make active."
          }
        },
        "required":[
          "session"
        ]
      }
    },
    {
      "action_name":"session.SET_EDITING_SESSION_NAME",
      "summary":"Sets which session name is currently being edited in the UI.",
      "payload_schema":{
        "type":"object",
        "properties":{
          "sessionId":{
            "type":[
              "string",
              "null"
            ]
          }
        }
      }
    },
    {
      "action_name":"session.SET_EDITING_MESSAGE",
      "summary":"Sets which message is currently being edited in the UI and pre-loads its content. Blocked if the target message is locked.",
      "payload_schema":{
        "type":"object",
        "properties":{
          "messageId":{
            "type":[
              "string",
              "null"
            ]
          }
        }
      }
    },
    {
      "action_name":"session.TOGGLE_MESSAGE_COLLAPSED",
      "summary":"Toggles the collapsed/expanded state for a single message.",
      "payload_schema":{
        "type":"object",
        "properties":{
          "sessionId":{
            "type":"string"
          },
          "messageId":{
            "type":"string"
          }
        },
        "required":[
          "sessionId",
          "messageId"
        ]
      }
    },
    {
      "action_name":"session.TOGGLE_MESSAGE_RAW_VIEW",
      "summary":"Toggles the raw/parsed view for a single message.",
      "payload_schema":{
        "type":"object",
        "properties":{
          "sessionId":{
            "type":"string"
          },
          "messageId":{
            "type":"string"
          }
        },
        "required":[
          "sessionId",
          "messageId"
        ]
      }
    },
    {
      "action_name":"session.REORDER",
      "summary":"Moves a session to a new position in the canonical display ordering used by both the tab bar and the session manager.",
      "payload_schema":{
        "type":"object",
        "properties":{
          "sessionId":{
            "type":"string",
            "description":"The ID of the session to move."
          },
          "toIndex":{
            "type":"integer",
            "description":"The target index in the ordering list. Clamped to valid bounds."
          }
        },
        "required":["sessionId", "toIndex"]
      }
    },
    {
      "action_name":"session.TOGGLE_HIDE_HIDDEN_IN_VIEWER",
      "summary":"Toggles whether hidden sessions are filtered out in the SessionView tab bar.",
      "payload_schema":{
        "type":"object",
        "properties":{}
      }
    },
    {
      "action_name":"session.TOGGLE_HIDE_HIDDEN_IN_MANAGER",
      "summary":"Toggles whether hidden sessions are filtered out in the SessionsManagerView.",
      "payload_schema":{
        "type":"object",
        "properties":{}
      }
    },
    {
      "action_name":"session.TOGGLE_SESSION_HIDDEN",
      "summary":"Toggles the isHidden flag on a specific session. Used from the session manager.",
      "payload_schema":{
        "type":"object",
        "properties":{
          "session":{
            "type":"string",
            "description":"The ID or name of the session to toggle."
          }
        },
        "required":["session"]
      }
    },
    {
      "action_name":"session.TOGGLE_MESSAGE_LOCKED",
      "summary":"Toggles the isLocked flag on a specific ledger entry. A locked message cannot be edited, deleted, or cleared.",
      "payload_schema":{
        "type":"object",
        "properties":{
          "sessionId":{
            "type":"string"
          },
          "messageId":{
            "type":"string"
          }
        },
        "required":["sessionId", "messageId"]
      }
    },
    {
      "action_name":"session.CLEAR",
      "summary":"Removes all non-locked messages from a session's ledger. Locked messages are preserved.",
      "payload_schema":{
        "type":"object",
        "properties":{
          "session":{
            "type":"string",
            "description":"The ID or name of the session to clear."
          }
        },
        "required":["session"]
      }
    }
  ],
  "publishes":[
    {
      "action_name":"session.publish.MESSAGE_POSTED",
      "summary":"Broadcasts that a new message has been successfully posted to a session.",
      "payload_schema":{
        "type":"object",
        "properties":{
          "sessionId":{ "type":"string" },
          "entry":{
            "type":"object",
            "description":"The full LedgerEntry object that was added.",
            "$ref":"#/definitions/LedgerEntry"
          }
        },
        "required":[ "sessionId", "entry" ]
      }
    },
    {
      "action_name":"session.publish.MESSAGE_DELETED",
      "summary":"Broadcasts that a message has been successfully deleted from a session.",
      "payload_schema":{
        "type":"object",
        "properties":{
          "sessionId":{ "type":"string" },
          "messageId":{ "type":"string" }
        },
        "required":[ "sessionId", "messageId" ]
      }
    },
    {
      "action_name":"session.publish.SESSION_UPDATED",
      "summary":"Broadcasts that a session has been updated (e.g., a new message was posted).",
      "payload_schema":{
        "type":"object",
        "properties":{
          "sessionId":{
            "type":"string"
          }
        },
        "required":[
          "sessionId"
        ]
      }
    },
    {
      "action_name":"session.publish.SESSION_DELETED",
      "summary":"Broadcasts that a session has been deleted, so other features can clean up their state.",
      "payload_schema":{
        "type":"object",
        "properties":{
          "sessionId":{
            "type":"string"
          }
        },
        "required":[
          "sessionId"
        ]
      }
    },
    {
      "action_name":"session.publish.SESSION_NAMES_UPDATED",
      "summary":"Broadcasts the verified map of session IDs to their names. Can only be dispatched by the SessionFeature.",
      "payload_schema":{
        "type":"object",
        "properties":{
          "names":{
            "type":"object",
            "description":"A map where the key is the session ID and the value is the session's name."
          }
        },
        "required":[
          "names"
        ]
      }
    },
    {
      "action_name":"session.internal.LOADED",
      "summary":"[INTERNAL] Dispatched after all session transcripts have been loaded from disk.",
      "payload_schema":{
        "type":"object",
        "description":"A map of session IDs to Session objects."
      }
    }
  ],
  "private_envelopes": [
    {
      "type_name": "session.response.ledger",
      "summary": "The response envelope containing the formatted ledger content for an agent's cognitive cycle.",
      "payload_schema": {
        "type": "object",
        "properties": {
          "correlationId": { "type": "string" },
          "messages": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "role": { "type": "string" },
                "content": { "type": "string" },
                "senderId": { "type": "string" },
                "senderName": { "type": "string" }
              },
              "required": ["role", "content", "senderId", "senderName"]
            }
          }
        },
        "required": ["correlationId", "messages"]
      }
    }
  ],
  "definitions": {
    "LedgerEntry": {
      "type": "object",
      "description": "Represents a single, immutable entry in a session's ledger.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for this entry."
        },
        "timestamp": {
          "type": "integer",
          "description": "Unix epoch milliseconds when the entry was created."
        },
        "senderId": {
          "type": "string",
          "description": "The identifier of the originator (e.g., 'user', an agent ID, 'commandbot', 'system')."
        },
        "rawContent": {
          "type": ["string", "null"],
          "description": "The original, unmodified string content of the message. Null for UI-only entries."
        },
        "content": {
          "type": "array",
          "description": "The structured, pre-parsed representation of rawContent as a list of ContentBlock objects.",
          "items": { "$ref": "#/definitions/ContentBlock" }
        },
        "metadata": {
          "type": ["object", "null"],
          "description": "A generic metadata object for UI hints or embedding components from other features."
        },
        "isLocked": {
          "type": "boolean",
          "description": "When true, this entry cannot be edited, deleted, or cleared. Defaults to false.",
          "default": false
        }
      },
      "required": ["id", "timestamp", "senderId"]
    },
    "ContentBlock": {
      "type": "object",
      "description": "A single block of structured content within a message. This is a sealed/discriminated union using the 'type' field.",
      "discriminator": "type",
      "oneOf": [
        { "$ref": "#/definitions/ContentBlock.Text" },
        { "$ref": "#/definitions/ContentBlock.CodeBlock" }
      ]
    },
    "ContentBlock.Text": {
      "type": "object",
      "description": "A block of plain or markdown text.",
      "properties": {
        "type": {
          "type": "string",
          "const": "app.auf.feature.session.ContentBlock.Text",
          "description": "Discriminator value for kotlinx.serialization sealed interface."
        },
        "text": {
          "type": "string",
          "description": "The text content."
        }
      },
      "required": ["type", "text"]
    },
    "ContentBlock.CodeBlock": {
      "type": "object",
      "description": "A fenced code block with a language tag. CommandBot watches for code blocks whose language starts with 'auf_' and dispatches the body as an action payload.",
      "properties": {
        "type": {
          "type": "string",
          "const": "app.auf.feature.session.ContentBlock.CodeBlock",
          "description": "Discriminator value for kotlinx.serialization sealed interface."
        },
        "language": {
          "type": "string",
          "description": "The language tag from the fenced code block (e.g., 'kotlin', 'auf_filesystem.SYSTEM_WRITE')."
        },
        "code": {
          "type": "string",
          "description": "The content of the code block."
        }
      },
      "required": ["type", "language", "code"]
    }
  }
}